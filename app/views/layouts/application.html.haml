!!! 5
%html{:lang => "pt-br"}
  %head
    %meta{:charset => "UTF-8"}
    = stylesheet_link_tag 'themes'
    %title= yield :title

  %body{:class => user_signed_in? ? current_user.theme : @user.theme}
    .all-content
      = yield

    %footer
      .align-center
        %p Sob.re | Todos os direitos reservados
      = render 'shared/google_analytics.html.erb'
      = yield :footer
